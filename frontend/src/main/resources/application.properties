spring.application.name=frontend
server.port=8080

# Security
spring.security.enabled=true

# OAuth2 Client для аутентификации пользователей через Keycloak (Authorization Code Flow)
spring.security.oauth2.client.provider.keycloak.issuer-uri=${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/bank}
spring.security.oauth2.client.provider.keycloak.authorization-uri=${KEYCLOAK_AUTHORIZATION_URI:http://localhost:8180/realms/bank/protocol/openid-connect/auth}
spring.security.oauth2.client.provider.keycloak.token-uri=${KEYCLOAK_TOKEN_URI:http://localhost:8180/realms/bank/protocol/openid-connect/token}
spring.security.oauth2.client.provider.keycloak.user-info-uri=${KEYCLOAK_USER_INFO_URI:http://localhost:8180/realms/bank/protocol/openid-connect/userinfo}
spring.security.oauth2.client.provider.keycloak.jwk-set-uri=${KEYCLOAK_JWK_SET_URI:http://localhost:8180/realms/bank/protocol/openid-connect/certs}
spring.security.oauth2.client.provider.keycloak.user-name-attribute=preferred_username
spring.security.oauth2.client.registration.frontend-client.client-id=${FRONTEND_CLIENT_ID:frontend-client}
spring.security.oauth2.client.registration.frontend-client.client-secret=${FRONTEND_CLIENT_SECRET:frontend-secret}
spring.security.oauth2.client.registration.frontend-client.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.frontend-client.redirect-uri=${FRONTEND_REDIRECT_URI:http://localhost:8081/login/oauth2/code/frontend-client}
spring.security.oauth2.client.registration.frontend-client.scope=openid,profile,email
spring.security.oauth2.client.registration.frontend-client.provider=keycloak

# OAuth2 Resource Server для валидации JWT токенов
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/bank}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${KEYCLOAK_JWK_SET_URI:http://localhost:8180/realms/bank/protocol/openid-connect/certs}

# Gateway service URL (default for local development, overridden by Consul in Docker)
gateway.service.url=http://localhost:8086

# Consul service discovery
spring.cloud.consul.enabled=true
spring.cloud.consul.host=${SPRING_CLOUD_CONSUL_HOST:localhost}
spring.cloud.consul.port=${SPRING_CLOUD_CONSUL_PORT:8500}
spring.cloud.consul.config.enabled=true
spring.cloud.consul.config.fail-fast=false
spring.cloud.consul.discovery.enabled=true
spring.cloud.consul.discovery.service-name=${spring.application.name}
spring.cloud.consul.discovery.instance-id=${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}
spring.cloud.consul.discovery.health-check-path=/actuator/health
spring.cloud.consul.discovery.health-check-interval=10s
spring.cloud.consul.discovery.health-check-timeout=5s
spring.cloud.consul.discovery.health-check-critical-timeout=30s
spring.cloud.consul.discovery.deregister=true
spring.cloud.consul.discovery.heartbeat.enabled=true
spring.cloud.consul.discovery.heartbeat.interval=5s

# Actuator
management.endpoints.web.exposure.include=health,info

# Logging
logging.level.ru.yandex.practicum.mybankfront=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2=DEBUG
