# Integration test configuration for Notifications Service
# Uses real PostgreSQL and Keycloak from Kubernetes cluster via port-forward

spring.application.name=notifications-service-integration

# Disable Spring Cloud Config and Consul for tests
spring.config.import=optional:none
spring.cloud.config.enabled=false
spring.cloud.consul.enabled=false
spring.cloud.consul.config.enabled=false
spring.cloud.consul.discovery.enabled=false

# Disable security for basic integration tests (use 'keycloak' profile for JWT tests)
spring.security.enabled=false

# Server configuration
server.port=0

# Disable OpenAPI for tests
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Kafka configuration (disabled for basic integration tests)
spring.kafka.bootstrap-servers=localhost:9092

# PostgreSQL Database (from Kubernetes via kubectl port-forward svc/postgresql 5432:5432)
spring.datasource.url=jdbc:postgresql://localhost:5432/bank?currentSchema=notifications
spring.datasource.username=user
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-mode=validate
spring.jpa.properties.hibernate.generate_statistics=false
spring.jpa.properties.hibernate.use_new_id_generator_mappings=false
spring.jpa.properties.hibernate.cache.use_second_level_cache=false
spring.jpa.properties.hibernate.cache.use_query_cache=false

# Logging
logging.level.ru.yandex.practicum.notifications=DEBUG
