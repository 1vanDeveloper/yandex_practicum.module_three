# Test configuration for Contract Tests
spring.application.name=notifications-service-contract-test

# Disable Consul for contract tests
spring.cloud.consul.enabled=false
spring.cloud.consul.discovery.enabled=false
spring.cloud.consul.config.enabled=false

# Server configuration
server.port=0

# Disable OpenAPI for contract tests
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# PostgreSQL Database for contract tests (from docker-compose)
spring.r2dbc.url=r2dbc:postgresql://localhost:5432/bank
spring.r2dbc.username=user
spring.r2dbc.password=password

# Disable OAuth2 for contract tests
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8180/realms/bank
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8180/realms/bank/protocol/openid-connect/certs
