spring.application.name=gateway
server.port=8080
spring.cloud.compatibility-verifier.enabled=false

# Virtual threads (Java 21+)
spring.threads.virtual.enabled=true

# ==========================================
# Service URLs (Kubernetes internal DNS)
# ==========================================
accounts.service.url=${ACCOUNTS_SERVICE_URL:http://accounts:8080}
cash.service.url=${CASH_SERVICE_URL:http://cash:8080}
transfer.service.url=${TRANSFER_SERVICE_URL:http://transfer:8080}
notifications.service.url=${NOTIFICATIONS_SERVICE_URL:http://notifications:8080}

# ==========================================
# JWT Configuration for User Token Validation
# ==========================================
jwt.secret=${JWT_SECRET:mySecretKeyForJWTTokenGenerationMustBeLongEnough}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ==========================================
# OAuth2 Resource Server (для валидации JWT от Keycloak)
# ==========================================
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://keycloak:8080/realms/bank}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${KEYCLOAK_JWK_SET_URI:http://keycloak:8080/realms/bank/protocol/openid-connect/certs}

# ==========================================
# OAuth2 Client Credentials (для межсервисных вызовов)
# ==========================================
spring.security.oauth2.client.provider.keycloak.token-uri=${KEYCLOAK_TOKEN_URI:http://keycloak:8080/realms/bank/protocol/openid-connect/token}
spring.security.oauth2.client.registration.gateway-service.client-id=${GATEWAY_CLIENT_ID:gateway-client}
spring.security.oauth2.client.registration.gateway-service.client-secret=${GATEWAY_CLIENT_SECRET:gateway-secret}
spring.security.oauth2.client.registration.gateway-service.authorization-grant-type=client_credentials
spring.security.oauth2.client.registration.gateway-service.provider=keycloak
spring.security.oauth2.client.registration.gateway-service.scope=accounts:modify,cash:modify,transfer:modify

# ==========================================
# Resilience4j Circuit Breaker Configuration
# ==========================================
resilience4j.circuitbreaker.configs.default.registerHealthIndicator=true
resilience4j.circuitbreaker.configs.default.slidingWindowSize=10
resilience4j.circuitbreaker.configs.default.minimumNumberOfCalls=5
resilience4j.circuitbreaker.configs.default.permittedNumberOfCallsInHalfOpenState=3
resilience4j.circuitbreaker.configs.default.automaticTransitionFromOpenToHalfOpenEnabled=true
resilience4j.circuitbreaker.configs.default.waitDurationInOpenState=10s
resilience4j.circuitbreaker.configs.default.failureRateThreshold=50
resilience4j.circuitbreaker.configs.default.eventConsumerBufferSize=10
resilience4j.circuitbreaker.configs.default.slidingWindowType=COUNT_BASED

# Resilience4j Retry Configuration
resilience4j.retry.configs.default.maxAttempts=3
resilience4j.retry.configs.default.waitDuration=1s
resilience4j.retry.configs.default.enableExponentialBackoff=true
resilience4j.retry.configs.default.exponentialBackoffMultiplier=2

# Resilience4j Timelimiter Configuration
resilience4j.timelimiter.configs.default.timeoutDuration=5s

# ==========================================
# Actuator (Health Checks)
# ==========================================
management.endpoints.web.exposure.include=health,info,routes
management.endpoint.health.show-details=always

# ==========================================
# Logging
# ==========================================
logging.level.ru.yandex.practicum.gateway=INFO
logging.level.org.springframework.cloud.gateway=INFO
logging.level.org.springframework.security=INFO

# ==========================================
# Micrometer Tracing (Zipkin)
# ==========================================
management.tracing.enabled=${MANAGEMENT_TRACING_ENABLED:true}
management.tracing.sampling.probability=${MANAGEMENT_TRACING_SAMPLING_PROBABILITY:1.0}
management.zipkin.tracing.endpoint=${MANAGEMENT_ZIPKIN_TRACING_ENDPOINT:http://zipkin:9411/api/v2/spans}
management.endpoints.web.exposure.include=health,info,prometheus,metrics

# Enable tracing for Spring Cloud Gateway
spring.cloud.gateway.httpclient.response-timeout=30s
management.observations.enabled=true
