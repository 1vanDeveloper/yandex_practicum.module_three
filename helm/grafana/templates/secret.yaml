apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secret
  labels:
    app: {{ .Chart.Name }}
type: Opaque
stringData:
  admin-user: {{ .Values.adminUser | quote }}
  admin-password: {{ .Values.adminPassword | quote }}
