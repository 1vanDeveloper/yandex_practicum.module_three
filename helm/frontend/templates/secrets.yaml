apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secrets
  labels:
    app: {{ .Chart.Name }}
type: Opaque
stringData:
  OAUTH2_CLIENT_SECRET: "{{ .Values.oauth2.clientSecret }}"
  KEYCLOAK_ISSUER_URI: "{{ .Values.keycloak.issuerUri }}"
  KEYCLOAK_JWK_SET_URI: "{{ .Values.keycloak.jwkSetUri }}"
