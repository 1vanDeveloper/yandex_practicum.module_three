apiVersion: v1
kind: Service
metadata:
  name: {{ include "elasticsearch.fullname" . }}
  labels:
    {{- include "elasticsearch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9200
      protocol: TCP
      name: http
    - port: {{ .Values.service.transportPort }}
      targetPort: 9300
      protocol: TCP
      name: transport
  selector:
    {{- include "elasticsearch.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "elasticsearch.fullname" . }}-headless
  labels:
    {{- include "elasticsearch.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9200
      protocol: TCP
      name: http
    - port: {{ .Values.service.transportPort }}
      targetPort: 9300
      protocol: TCP
      name: transport
  selector:
    {{- include "elasticsearch.selectorLabels" . | nindent 4 }}
